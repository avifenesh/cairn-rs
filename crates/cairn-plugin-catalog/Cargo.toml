[package]
name = "cairn-plugin-catalog"
version = "0.1.0"
edition = "2021"
license = "Proprietary"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = { version = "0.8", default-features = false, features = ["parse", "display"] }

[dev-dependencies]
