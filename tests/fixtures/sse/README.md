# SSE Fixtures

Store preserved or transitional SSE payload fixtures here.

Preferred filename pattern:

- `<event-name>__<scenario>.json`

Examples:

- `feed_update__single_item.json`
- `assistant_delta__incremental_reply.json`
