# RFC 017 Patch v1 — Review Summary

**File**: `docs/design/rfcs/017-github-reference-plugin.md`
**Previous size**: 384 lines · **New size**: 441 lines (+57)
**Findings addressed**: 11 (3 broadcast + 8 deep-read, with 5 triple-confirmed)

## Items

1. **F-arg0 + F-W1-13 + F-W3-17 [H]** — arg0/bundled-binary scrub: catalog entry `bundled://` → `download_url`, Open Q6 resolved as external binary, Decision bullet rewritten, homepage → independent repo, Resolved Decisions text confirmed
2. **F-W1-04 [H]** — webhook secret: 4-credential model (3 tenant + 1 project), `github_webhook_secret` added to manifest `required_credentials` with `scope = Tenant`, verification endpoint uses tenant-scoped App JWT not project-scoped installation token
3. **F-W1-06 [M]** — signal subscription: Open Q1 resolved with RFC 022 Trigger cross-reference, dogfood path rewritten to show Trigger + RunTemplate
4. **F-COMBINED-14 [H]** — connect/PluginConnected lifecycle scrub: line 11 discover→install, line 71 no connect step, test 2 → PluginCredentialsVerified + PluginInstanceReady, Decision bullet no connect/no PluginConnected
5. **F-COMBINED-15 [M]** — memory_ingest hints: 3 `[[capabilities.memory_ingest]]` entries for issue.opened/labeled/pr.opened, graph_projection comment, pr.synchronize deliberately omitted
6. **F-W1-16 [M]** — rate-limit: normal signal subject to signal_allowlist, not implicit delivery; health UI for unconditional observability
7. **F-W1-17 [M]** — OAuth/manual-token: deleted OAuth block, v1 is App credential wizard only, health check uses `github.verify_app_auth` (App JWT)
8. **F-W2-08 [H]** — webhook dedup: durable via RFC 022 signal-delivery ledger, not 24-hour memory cache; test 6 exercises restart-survival
9. **F-W2-10 [M]** — health check scope: install/verify uses tenant-scoped App JWT (`GET /app`), not project-scoped installation token
10. **F-W3-20 [M]** — source_run_id: added to signal normalization envelope, mutating tools embed `<!-- cairn:run_id=... -->` marker, loop-back detection via bot-user check, test 15 covers chain-depth
11. **F-W3-21 [L]** — dogfood path: added RepoStore allowlist grant step (step 6)

## Sign-off tags

```
[SIGNOFF arg0] [SIGNOFF W1-04] [SIGNOFF W1-06] [SIGNOFF COMBINED-14]
[SIGNOFF COMBINED-15] [SIGNOFF W1-16] [SIGNOFF W1-17] [SIGNOFF W2-08]
[SIGNOFF W2-10] [SIGNOFF W3-20] [SIGNOFF W3-21] [SIGNOFF SURFACE-SCRUB]
```

Review window: 20 minutes.
