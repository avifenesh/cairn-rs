# RFC 016 Patch v1 — Review Summary

**File under review**: `docs/design/rfcs/016-sandbox-workspace-primitive.md`
**Previous size**: 761 lines · **New size**: 1292 lines (+531)
**Findings addressed**: 11 (1 from broadcast scope + 10 surfaced during deep re-read triage)

Read the full updated file and compare against your pre-scan / triage recall.
Sign off per item using `[SIGNOFF ...]` / `[OBJECT ...: reason]`.

---

## Item 1 — F-W2-01 [H] Project-scoped access allowlist + tenant-scoped cache split

**Sections touched**: Summary (line 11), In-scope list (lines 117-136), §RepoStore (entirely rewritten, lines ~456-700+), §cairn.registerRepo, §RepoStore events, Storage layout, Reconnect on Restart, Decision bullets, Integration tests 1/6/7/17/18/19/20

**Design decisions**: RepoCloneCache (tenant-scoped) + ProjectRepoAccessService (project-scoped) + optional RepoStore facade. RepoAccessContext { project: ProjectKey } in cairn-domain. Storage path adds {tenant_id} segment. path() is internal-only. cairn.registerRepo returns authorization status, not host path. Project-level keying. v0→v1 migration via SystemMigration events.

---

## Item 2 — F-COMBINED-10 [H] Worktree leak scrub (triple-confirmed)

**Sections touched**: Line 128 (state count 7→9), line 183 (Provisioning description), SandboxPolicyDegraded reason string, Storage layout (git_worktree block deleted, meta.json rewritten), Open Question 7 (deleted as dead), all remaining worktree references verified as intentional anti-references

---

## Item 3 — F-COMBINED-11 [M] Resource-limit events v2.1

**Sections touched**: SandboxEvent enum (new SandboxResourceLimitExceeded variant), new ResourceDimension enum (DiskBytes/MemoryBytes/WallClockMs), new formal DestroyReason + PreservationReason enums with limit/observed on reason variants, new OnExhaustion enum on SandboxPolicy, new §"Resource-exhaustion runtime flow" section, Open Question 6 promoted to resolved decision, integration test 12 rewritten for three modes

---

## Item 4 — F-W2-05 [M] Startup ordering

**Sections touched**: §"Reconnect on Restart" — deleted stale "first, then" wording; kept parallel-where-independent as canonical

---

## Item 5 — F-W1-12 [M] Operator HTTP contract + async GC

**Sections touched**: New §"Operator HTTP contract for project repo access" with 4 routes (:owner/:repo split), new §"Async physical clone GC" with ActiveSandboxRepoSource trait + RepoCloneSweepTask + sweep events + placement, integration test 17

---

## Item 6 — F-W3-11 [M] cairn-graph projection for sandbox events

**Sections touched**: New §"Integration Points → With RFC 004" section with opaque typed node IDs (sandbox:<id>, repobase:<tenant>:<repo>@<rev>, dirbase:<alias>), edge list, failure-path projection, DirectoryBase alias condition. Integration test 15 extended.

---

## Item 7 — F-W3-12 [L] RepoStore auto-memory-indexing non-goal

**Sections touched**: Non-Goals section — new bullet

---

## Item 8 — F-W3-13 [M] Integration test renumbering

**Sections touched**: Integration Tests section — renumbered to 1-20 (was 1-12 with duplicate 4/5/6/7)

---

## Item 9 — F-W3-14 [L] SandboxBase::Directory access invariant

**Sections touched**: New §"SandboxBase::Directory access invariant (v1)" section after the SandboxBase enum, Non-Goals bullet for agent-driven SandboxBase selection

---

## Item 10 — F-W3-15 [M] Locked clone immutability + drift detection

**Sections touched**: New §"Locked clone immutability invariant" section with refresh() sequence, new §"Reflink snapshot-semantics requirement" section, SandboxBaseRevisionDrift event, PreservationReason::BaseRevisionDrift variant, recovery flow step 2b (overlay-only base_rev check), recovery flow step 2a (allowlist-revoked check), integration tests 18/19

---

## Item 11 — Surface scrub

**Sections touched**: Summary rewrite, In-scope list refresh, Decision bullets rewrite, all cross-references updated

---

## Sign-off Protocol

Reply via `./scripts/team-send.sh manager worker-N "body"` with per-item tags:

```
[SIGNOFF W2-01]    — or — [OBJECT W2-01: <concern>]
[SIGNOFF COMBINED-10]
[SIGNOFF COMBINED-11]
[SIGNOFF W2-05]
[SIGNOFF W1-12]
[SIGNOFF W3-11]
[SIGNOFF W3-12]
[SIGNOFF W3-13]
[SIGNOFF W3-14]
[SIGNOFF W3-15]
[SIGNOFF SURFACE-SCRUB]
```

Protocol reminder: **fix → all re-read**. Do not sign off on previous-round mental models — re-read the new text and verify each fix covers your concern.

Review window: 25 minutes (larger patch than RFC 015).
