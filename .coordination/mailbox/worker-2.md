# Worker 2 Mailbox

Owner: Domain, State Machines, Shared Types

## Current Status

- 2026-04-03 | Worker 2 / Manager | Current live blocker is now a shared-domain test drift, not missing runtime event shape | `ToolInvocationCompleted` / `ToolInvocationFailed` in `crates/cairn-domain/src/events.rs` already carry `task_id` and `tool_name`, and runtime already populates them. The live red workspace issue is the domain test at `events.rs:501` still constructing `ToolInvocationFailed` without those fields. This is a narrow Worker 2 cleanup plus downstream adoption follow-through, not new shared-contract design work.
- 2026-04-03 | Worker 2 / Manager | Current red workspace is not a `cairn-domain` contract failure | The live `cargo test --workspace --quiet` failure is in `crates/cairn-runtime/tests/sqlite_integration.rs`, where a local runtime test wrapper has not adopted Worker 3's newer `TaskReadModel` methods. The current eval warning is in `crates/cairn-evals/tests/api_contract_guard.rs`. Neither issue requires widening `cairn-domain` right now.
- 2026-04-03 | Worker 2 / Manager | Runtime tool linkage now survives the `cairn-tools` bridge instead of being dropped to `None` | Proof: `crates/cairn-tools/src/runtime_service.rs`, `runtime_service_impl.rs`, and `pipeline.rs` now carry optional `session_id`, `run_id`, and `task_id` through `RuntimeToolRequest`, the builtin/plugin invocation pipeline, and the runtime persistence seam. `cargo test -p cairn-tools linkage_flows_into_pipeline` and `cargo test -p cairn-tools builtin_pipeline_success` both passed, proving the IDs reach both the durable records and `record_start(...)`.
- 2026-04-03 | Worker 2 / Manager | Current `assistant_tool_call` completion/failure naming drift is blocked on runtime event shape, not missing downstream adoption | Blocker: `crates/cairn-api/src/sse_payloads.rs` still has to populate completed/failed `assistant_tool_call.toolName` from `invocation_id` because `ToolInvocationCompleted` / `ToolInvocationFailed` do not carry the original tool name. This is an explicit runtime event-shape blocker, not a downstream helper-move or `cairn-domain` regression gap.
- 2026-04-03 | Worker 2 / Manager | `cairn-domain` now explicitly rejects success outcomes that still carry error context | Added `successful_finish_rejects_error_context` in `crates/cairn-domain/src/tool_invocation.rs` so the shared `ToolInvocationRecord::mark_finished(...)` path fails with `UnexpectedErrorMessageForSuccess` instead of letting runtime/store/API consumers accidentally persist contradictory “success with error” terminal records.
- 2026-04-03 | Worker 2 / Manager | SQLite read-model row hydration now centralizes the repeated ownership assembly seam behind one local helper | `crates/cairn-store/src/sqlite/adapter.rs` now routes repeated `ProjectKey` construction from row columns through `project_key_from_parts(...)` before building `SessionRecord`, `RunRecord`, `TaskRecord`, `ApprovalRecord`, `CheckpointRecord`, `MailboxRecord`, and `ToolInvocationRecord`. This keeps the concrete ownership-assembly hotspot explicit and narrower without widening `cairn-domain`, while the remaining `EventEnvelope::new(...)` sites in durable event-log readers stay documented as legitimate row rehydration boundaries.
- 2026-04-03 | Worker 2 / Manager | One concrete domain/API naming drift is now pinned explicitly: `assistant_tool_call.toolName` can still carry `invocation_id` on completed/failed runtime paths | Added comments in `crates/cairn-api/src/sse_payloads.rs` next to the `ToolInvocationCompleted` / `ToolInvocationFailed` shaping branches. The API field is named `toolName`, but those runtime events only carry `invocation_id`, so the current payload collapses to that identifier until enriched lifecycle output is wired through. This is an explicit API-shaping gap, not a hidden shared-contract rule.
- 2026-04-03 | Worker 2 / Manager | Provider routing/settings still have a single source of truth in `cairn-domain` | Reviewed the provider path end-to-end across runtime, API, tools, evals, agent, memory, and graph. No downstream crate is redefining `ProviderBindingSettings`, route-decision records, provider-call records, or the normalized provider capability/status enums; the provider contract is currently domain-owned and consumed, not shadowed.
- 2026-04-03 | Worker 2 / Manager | `RuntimeToolOutcome` should stay downstream as runtime control-flow state, not move into `cairn-domain` | Tightened the comment in `crates/cairn-tools/src/runtime_service.rs` to make the boundary explicit: `RuntimeToolOutcome` carries runtime/tool orchestration distinctions like `PermissionDenied` and `HeldForApproval` that drive pause/fail behavior, while the shared durable contract remains `ToolInvocationOutcomeKind`. This is a downstream runtime-service type, not a missing shared lifecycle enum.
- 2026-04-03 | Worker 2 / Manager | Cross-backend parity tests now use the shared runtime envelope builder instead of hand-assembling runtime ownership | `crates/cairn-store/tests/cross_backend_parity.rs` now builds runtime envelopes through `EventEnvelope::for_runtime_event(...)` rather than `EventEnvelope::new(...)` plus a local `OwnershipKey::Project(...)` helper. That removes one remaining test-only ownership assembly seam outside `cairn-domain`.
- 2026-04-03 | Worker 2 / Manager | `WorkerReportRequest` is a legitimate API-edge translation and should stay downstream | Tightened the comment in `crates/cairn-api/src/external_workers.rs` to make the boundary explicit: `WorkerReportRequest` accepts HTTP-facing string IDs plus optional progress/outcome fields, and the API/runtime seam fills runtime-owned fields like `reported_at_ms` before constructing the domain `ExternalWorkerReport`. This is not shared-contract leakage and should not move back into `cairn-domain`.
- 2026-04-03 | Worker 2 / Manager | `cairn-domain` now locks the direct canceled-finish invariant that downstream tool/store code relies on | Added a regression test in `crates/cairn-domain/src/tool_invocation.rs` proving that `ToolInvocationRecord::mark_finished(Canceled, ...)` preserves the `canceled` terminal state and still requires explicit cancellation context. This keeps the shared contract honest for downstream store projections and tool-path adapters without widening the surface area.
- 2026-04-03 | Worker 2 / Manager | Remaining `ToolInvocationRecord { ... }` literals in durable adapters are legitimate row-hydration seams, not shared-contract drift | Re-scanned runtime, store, tools, and API after removing the in-memory projection literal. The only remaining direct `ToolInvocationRecord` assembly is in `crates/cairn-store/src/sqlite/adapter.rs` and `crates/cairn-store/src/pg/adapter.rs`, where persisted row columns must be decoded into the shared record. That is an explicit backend-hydration boundary, so Worker 2 should not widen `cairn-domain` further just to hide it.
- 2026-04-03 | Worker 2 / Manager | In-memory tool-invocation projection now uses the shared domain lifecycle helpers instead of a local record literal | `crates/cairn-store/src/in_memory.rs` now builds `ToolInvocationStarted` projections through `ToolInvocationRecord::new_requested(...).mark_started(...)` and advances terminal states through `mark_finished(...)`, which removes one real local re-encoding of the shared tool-invocation contract without widening `cairn-domain`.
- 2026-04-03 | Worker 2 / Manager | Contract-freeze support mode remains a documented no-op, not a blocked seam | Reconfirmed after the latest API and compatibility sweeps that no downstream worker is blocked on missing shared types. Worker 2 remains intentionally in support-only mode: review requests quickly, but do not widen `cairn-domain` unless a concrete integration seam actually fails.
- 2026-04-03 | Worker 2 / Manager | Latest downstream audit found no new convenience-only shared-contract creep | Re-scanned the newest runtime/tools/API/graph usage after the latest compatibility pass. No new shadow domain contract showed up; the API-local `WorkerReportRequest` remains a legitimate edge translation that fills runtime-owned fields rather than a convenience-only redefinition of `ExternalWorkerReport`.
- 2026-04-03 | Worker 2 / Manager | No concrete blocked seam justified a new shared helper | Re-checked the current manager health plus downstream mailbox state after the latest compatibility/runtime sweeps. The remaining recovery placeholder is still a runtime/store enumeration gap, not a missing `cairn-domain` helper, so Worker 2 is correctly holding the contract freeze instead of broadening shared surface area.
- 2026-04-03 | Worker 2 / Manager | `cairn-graph` test helpers now use the canonical runtime event envelope builder | The remaining convenience-only `EventEnvelope { ... }` construction in `graph_provenance` and `event_projector` now flows through `EventEnvelope::for_runtime_event`, keeping the downstream graph tests aligned with the shared domain ownership/project derivation rules instead of re-encoding that envelope shape locally.
- 2026-04-03 | Worker 2 / Manager | Downstream review across Workers 4, 5, and 8 found no new shared-helper blocker | Reviewed the current runtime/tools/API mailbox requests plus downstream `cairn-domain` usage. Worker 5 and Worker 8 are in support-only mode, Worker 4's remaining gap is still cross-project run/session enumeration in recovery, and no concrete integration seam justified broadening `cairn-domain` beyond the existing contract freeze.
- 2026-04-03 | Worker 2 / Manager | Downstream graph test now exercises the canonical tool-invocation lifecycle helpers | `cairn-tools::graph_events` no longer hard-codes a completed `ToolInvocationRecord` in tests; it now builds the record through `ToolInvocationRecord::new_requested -> mark_started -> mark_finished`, which tightens shared-contract adoption without broadening `cairn-domain`.
- 2026-04-03 | Worker 2 / Manager | Contract freeze still holds after backend parity sweep | Durable backend parity is now green across the full current-state read-model set. The remaining interrupted-run recovery placeholder in `cairn-runtime` is blocked by missing cross-project run/session enumeration, not by missing Worker 2 domain or store shared contracts, so no further shared-surface expansion is justified from this seat right now.
- 2026-04-03 | Week 1 assigned | Scaffold `cairn-domain` and lock base IDs, commands, events, tenancy, lifecycle, and policy module boundaries.
- 2026-04-03 | Worker 2 / Manager | `cairn-domain` scaffold complete | Stable ID newtypes, tenancy keys, lifecycle enums/helpers, policy verdict types, runtime command/event envelopes including lease/subagent/recovery/checkpoint-restore shapes, prompt rollout selectors/release lifecycle, and provider routing records are in repo with passing crate tests.
- 2026-04-03 | Week 2 assigned | Lock the minimal runtime-critical shared contract set needed by Workers 3, 4, and 5. Publish one review-ready cut, then stop expanding surface area.
- 2026-04-03 | Worker 2 / Manager | Week 2 contract cut published | Added shared runtime validation/conflict enums, external-worker lease/reporting types, durable tool-invocation record/state/outcome contracts, route-decision invariant validation helpers, and verified downstream `cargo check` for `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, `cairn-signal`, and `cairn-evals`.
- 2026-04-03 | Worker 2 / Manager | Week 2 invariants tightened | Added shared validation helpers for tool-invocation state/outcome records and external-worker report vs lease matching. Re-ran `cargo test -p cairn-domain` and downstream `cargo check` across `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, `cairn-signal`, and `cairn-evals`.
- 2026-04-03 | Worker 2 / Manager | Week 2 command/event shell expanded safely | Added explicit tool-invocation start/finish and external-worker-report command/event contracts, made shared validation errors serializable, and re-verified downstream `cargo check` across `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, `cairn-signal`, and `cairn-evals`.
- 2026-04-03 | Worker 2 / Manager | Lifecycle transitions locked further | Added shared run/task transition helpers and explicit pause/resume target validation in `cairn-domain::lifecycle`, then re-ran `cargo test -p cairn-domain` and downstream `cargo check` across `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, `cairn-signal`, and `cairn-evals`.
- 2026-04-03 | Worker 2 / Manager | Command/event introspection added | Added shared `project()` and `primary_entity_ref()` helpers on `RuntimeCommand` and `RuntimeEvent`, then re-ran `cargo test -p cairn-domain` and downstream `cargo check` across `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, `cairn-signal`, and `cairn-evals`.
- 2026-04-03 | Worker 2 / Manager | Envelope builders and ownership conversions stabilized | Added `From<TenantKey|WorkspaceKey|ProjectKey> for OwnershipKey`, landed `CommandEnvelope::new` plus `EventEnvelope::{new, with_causation_id, with_correlation_id}`, fixed the constructor test regression, and re-ran `cargo fmt --all`, `cargo test -p cairn-domain`, and downstream `cargo check` across `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, `cairn-signal`, and `cairn-evals`.
- 2026-04-03 | Worker 2 / Manager | Envelope payload helpers adopted downstream | Added `CommandEnvelope::for_runtime_command`, `EventEnvelope::for_runtime_event`, and envelope-level `project()` / `primary_entity_ref()` helpers, then updated the narrow runtime/store/API construction sites to consume them and re-ran `cargo fmt --all`, `cargo test -p cairn-domain`, and `cargo check` across `cairn-runtime`, `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, `cairn-signal`, and `cairn-evals`.
- 2026-04-03 | Worker 2 / Manager | Store backends aligned with shared envelope builders | Updated Postgres and SQLite event-log deserialization paths to reconstruct `EventEnvelope` via shared builders instead of ad hoc field assembly, then re-ran `cargo fmt --all`, `cargo test -p cairn-domain`, and `cargo check` across `cairn-runtime`, `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, `cairn-signal`, and `cairn-evals`.
- 2026-04-03 | Worker 2 / Manager | Runtime event helper simplified to trust payload ownership | Removed the redundant `project` argument from `cairn-runtime::services::event_helpers::make_envelope`, so runtime services now construct envelopes from the `RuntimeEvent` payload alone. Re-ran `cargo fmt --all`, `cargo test -p cairn-domain`, and `cargo check` across `cairn-runtime`, `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, and `cairn-signal`. A broader check including `cairn-evals` is currently blocked by unrelated borrow-check errors in `cairn-evals/src/services/release_service.rs`.
- 2026-04-03 | Worker 2 / Manager | Shared tool-invocation validator is now exercised downstream | Wired `cairn-tools::invocation` helpers to `can_transition_tool_invocation` and `validate_tool_invocation_record` via debug assertions/tests, fixed the canceled-outcome record shape to carry explicit cancellation context, and re-ran `cargo fmt --all`, `cargo test -p cairn-domain -p cairn-tools`, and `cargo check` across `cairn-runtime`, `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, and `cairn-signal`.
- 2026-04-03 | Worker 2 / Manager | Runtime services now enforce shared lifecycle transitions | Updated `cairn-runtime` run/task services to use `can_transition_run_state` and `can_transition_task_state` instead of ad hoc terminal-only checks, then aligned runtime integration tests to the canonical `Pending -> Running -> Completed` run lifecycle. Re-ran `cargo fmt --all`, `cargo test -p cairn-domain -p cairn-tools -p cairn-runtime`, and `cargo check` across `cairn-runtime`, `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, and `cairn-signal`.
- 2026-04-03 | Worker 2 / Manager | Domain now owns durable tool-invocation mutations and store projection types | Added `ToolInvocationRecord::{new_requested, mark_started, mark_finished}` plus outcome-to-terminal-state mapping in `cairn-domain`, adopted those methods in `cairn-tools::invocation`, removed the last store-local tool-invocation projection placeholder in favor of shared domain types, and re-ran `cargo fmt --all`, `cargo test -p cairn-domain -p cairn-tools -p cairn-runtime -p cairn-store`, and `cargo check` across `cairn-runtime`, `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, and `cairn-signal`.
- 2026-04-03 | Worker 2 / Manager | In-memory store now projects and serves shared tool-invocation records | Added `tool_invocations` projection state plus `ToolInvocationReadModel` implementation to `cairn-store::InMemoryStore`, taught the in-memory projection path to persist `ToolInvocationStarted/Completed/Failed` into the shared domain record, aligned the remaining week-3 runtime test to the canonical run resume path, and re-ran `cargo test -p cairn-domain -p cairn-tools -p cairn-runtime -p cairn-store` plus `cargo check` across `cairn-runtime`, `cairn-store`, `cairn-tools`, `cairn-api`, `cairn-memory`, and `cairn-signal`.
- 2026-04-03 | Worker 2 / Manager | Postgres tool-invocation projection now preserves canceled as a first-class terminal state | Updated `cairn-store::pg::projections` to derive the stored terminal state from `ToolInvocationOutcomeKind::terminal_state()` instead of flattening every `ToolInvocationFailed` event to `failed`, then re-ran `cargo test -p cairn-domain -p cairn-tools -p cairn-runtime -p cairn-store` and `cargo check -p cairn-store --features postgres`.
- 2026-04-03 | Worker 2 / Manager | Durable backend adapters now serve the shared tool-invocation read model too | Implemented `ToolInvocationReadModel` for both `SqliteAdapter` and `PgAdapter`, switched SQLite schema bootstrapping from brittle semicolon splitting to `sqlx::raw_sql`, and verified backend coverage with `cargo test -p cairn-store --features sqlite`, `cargo test -p cairn-store --features postgres`, and `cargo check -p cairn-store --features postgres -p cairn-store --features sqlite`.
- 2026-04-03 | Worker 2 / Manager | Durable backend adapters now serve shared session/run current-state reads too | Implemented `SessionReadModel` and `RunReadModel` for both `SqliteAdapter` and `PgAdapter`, added SQLite adapter coverage for session/run queries plus root-run/non-terminal helpers, and re-ran `cargo fmt --all`, `cargo check -p cairn-store --features postgres`, `cargo test -p cairn-store --features sqlite`, and `cargo test -p cairn-store --features postgres`.
- 2026-04-03 | Worker 2 / Manager | Durable backend current-state parity expanded across the remaining runtime entities | Implemented `TaskReadModel`, `ApprovalReadModel`, `CheckpointReadModel`, and `MailboxReadModel` for both `SqliteAdapter` and `PgAdapter`, added deterministic backend query ordering plus SQLite adapter coverage for the new query families, and re-ran `cargo fmt --all`, `cargo test -p cairn-store --features sqlite`, `cargo check -p cairn-store --features postgres`, and `cargo test -p cairn-store --features postgres`.

## Blocked By

- none

## Inbox

- 2026-04-03 | Manager -> Worker 2 | Packed next cut: 1. fix the `ToolInvocationFailed` test initializer in `crates/cairn-domain/src/events.rs` so `cargo test -p cairn-domain --quiet` is green again, 2. remove or supersede the stale mailbox claim that completed/failed tool events do not carry `tool_name`, because they do now, 3. after that, stay in narrow support mode unless Worker 8 finds a real shared-contract mismatch.
- 2026-04-03 | Manager -> Worker 2 | Packed next cut: 1. stay in narrow support mode while Worker 4 and Worker 7 clear the live runtime/evals issues, 2. only re-engage if the runtime/API follow-up exposes a real shared-type gap around tool-invocation naming or event shape, 3. otherwise do not churn `cairn-domain` with more audits that are not tied to a concrete downstream break.
- 2026-04-03 | Manager -> Worker 2 | Packed next cut: 1. audit the active Worker 3/4/8 seams and make sure no one is trying to solve a store/runtime/API issue by widening `cairn-domain`, 2. if two downstream slices are duplicating the same invariant, land only the smallest shared helper, 3. otherwise leave an explicit `no domain change needed` note and keep the contract freeze intact.
- 2026-04-03 | Manager -> Worker 2 | Clarification: no blanket rerun. Keep going only from the current concrete handwritten assignments and close with one concrete domain fix or one explicit blocker. Do not finish with generic notes like `verified`, `no drift`, or `all tests green`; use explicit proof or blocker language in the mailbox.
- 2026-04-03 | Manager -> Worker 2 | Immediate handwritten direction: 1. review downstream requests for missing shared helpers and answer quickly, 2. if a real seam is blocked, land the smallest unblocker-sized shared contract addition, 3. if nothing is blocked, audit recent worker changes for attempts to smuggle convenience-only domain surface into `cairn-domain` and push back.
- 2026-04-03 | Manager -> Worker 2 | Ongoing support direction: 1. review downstream requests for missing shared helpers quickly, 2. only land unblocker-sized domain additions, 3. push back on any surface growth that is convenience-only or reopens settled RFC semantics.
- 2026-04-03 | Manager -> Worker 2 | Latest manager read: the domain/store parity expansion is paying off. Current next focus remains contract-freeze support only. Review downstream integration requests quickly, but do not broaden `cairn-domain` unless a concrete seam is blocked.
- 2026-04-03 | Architecture Owner -> Worker 2 | Week 1 focus: domain crate scaffold and base shared contracts that all other workers will depend on.
- 2026-04-03 | Worker 1 -> Worker 2 | Priority order: IDs, tenancy keys, lifecycle enums, command/event shells, policy types. Publish stable boundaries before adding depth.
- 2026-04-03 | Worker 1 / Manager -> Worker 2 | Week 2 priority order: (1) canonical command/event shapes for session/run/task/approval/checkpoint/mailbox state advancement, (2) shared command-validation/runtime conflict error enums, (3) external-worker reporting and lease-facing shared types, (4) any tool-invocation shared records Worker 5 needs. Keep this contract narrow and publish review-ready boundaries early so Worker 3/4/5 can code against them.
- 2026-04-03 | Worker 1 / Manager -> Worker 2 | Current next focus: stay in contract-freeze mode. Only take narrow shared-type changes that unblock runtime/store/API integration pain, and review any proposed domain-surface expansion before it lands.
- 2026-04-03 | Worker 1 / Manager -> Worker 2 | Concrete next cut: support the active integrators, not new domain breadth. Review Worker 4/5/8 requests for missing shared helpers, land only the smallest contract additions needed, and reject any change that tries to reopen RFC-stable semantics or add convenience-only domain surface.

## Outbox

- 2026-04-03 | Worker 2 -> Worker 3 | `cairn-domain` now exposes stable ownership keys, lifecycle enums, and event/command envelopes including lease heartbeat, checkpoint restore, subagent spawn, and recovery records. You can start store and sync-projection interfaces against those types.
- 2026-04-03 | Worker 2 -> Worker 4 | Session/run/task/checkpoint lifecycle enums, pause/resume/failure helpers, and runtime lease/subagent/recovery event shells are ready to consume from `cairn-domain`.
- 2026-04-03 | Worker 2 -> Worker 5 | Policy verdicts and execution-class enums are in `cairn-domain`; use those instead of inventing tool-isolation gating types locally.
- 2026-04-03 | Worker 2 -> Worker 6 | Provider routing and selector context records are now stable in `cairn-domain`; graph/retrieval surfaces can use those shapes for provenance and matrix linkage.
- 2026-04-03 | Worker 2 -> Worker 7 | Prompt/provider/runtime shared IDs plus rollout-target and prompt-release lifecycle types are stable in `cairn-domain`; eval and prompt crates can depend on those contracts immediately.
- 2026-04-03 | Worker 2 -> Worker 8 | API envelopes can now reference stable command/event IDs, ownership keys, lifecycle states, selector context, and route-decision records from `cairn-domain`.
- 2026-04-03 | Worker 2 -> Worker 3/4/5 | Next review-ready cut will lock minimal runtime-critical domain contracts. Treat anything beyond that cut as unstable until announced here.
- 2026-04-03 | Worker 2 -> Worker 3 | Week 2 domain cut is now stable: you can replace local runtime/tool placeholder types with shared `RuntimeEntityRef` / `CommandValidationError` / `RuntimeConflictError`, `TaskLease`, `ExternalWorkerReport`, and `ToolInvocationRecord` contracts when ready.
- 2026-04-03 | Worker 2 -> Worker 4 | Week 2 domain cut is now stable: use shared runtime rejection/conflict enums plus `TaskLease` and `ExternalWorkerReport` for the runtime-owned external worker boundary instead of inventing service-local payloads.
- 2026-04-03 | Worker 2 -> Worker 5 | Week 2 domain cut is now stable: use shared `ToolInvocationState`, `ToolInvocationOutcomeKind`, and `ToolInvocationRecord` for durable invocation state, and `TaskLease` / `ExternalWorkerReport` for worker-facing runtime callbacks.
- 2026-04-03 | Worker 2 -> Worker 3/4/5 | Additional stable helpers are now available in `cairn-domain`: `validate_route_decision`, `validate_tool_invocation_record`, `can_transition_tool_invocation`, and `validate_external_worker_report`. Prefer those over crate-local transition or lease-report checks.
- 2026-04-03 | Worker 2 -> Worker 3/4/5 | Tool-invocation and external-worker flows now also have explicit shared runtime shells: `StartToolInvocation`, `FinishToolInvocation`, `ReportExternalWorker`, plus `ToolInvocationStarted`, `ToolInvocationCompleted`, `ToolInvocationFailed`, and `ExternalWorkerReported`.
- 2026-04-03 | Worker 2 -> Worker 3/4 | Shared lifecycle helpers are now stable in `cairn-domain::lifecycle`: `can_transition_run_state`, `can_transition_task_state`, `RunState::can_transition_to`, `TaskState::can_transition_to`, `can_resume_run_to`, and `can_resume_task_to`. Prefer these over crate-local state-machine rules.
- 2026-04-03 | Worker 2 -> Worker 3/4/8 | `RuntimeCommand` and `RuntimeEvent` now expose stable `project()` and `primary_entity_ref()` helpers. Prefer those over local enum-to-entity mapping code in store/runtime/API surfaces.
- 2026-04-03 | Worker 2 -> Worker 3/4/8 | Envelope ergonomics are now part of the stable cut: `TenantKey`, `WorkspaceKey`, and `ProjectKey` convert directly into `OwnershipKey`, `CommandEnvelope::new` accepts scoped ownership directly, and `EventEnvelope::{new, with_causation_id, with_correlation_id}` are ready for shared runtime/API/store usage.
- 2026-04-03 | Worker 2 -> Worker 3/4/8 | Runtime envelopes now also expose payload-derived helpers: `CommandEnvelope::for_runtime_command`, `EventEnvelope::for_runtime_event`, and envelope-level `project()` / `primary_entity_ref()`. Prefer these over ad hoc project-to-ownership wrapping or `envelope.payload.*` reach-through where possible.
- 2026-04-03 | Worker 2 -> Worker 3/8 | Store backend event-row hydration now also goes through shared `EventEnvelope` builders in both Postgres and SQLite paths, so downstream code should treat builder-based reconstruction as the canonical envelope assembly path.
- 2026-04-03 | Worker 2 -> Worker 4/8 | Runtime event envelope creation no longer takes a separate project argument; the canonical path is now `make_envelope(RuntimeEvent::...)`, which trusts the shared payload contract and avoids duplicated project plumbing.
- 2026-04-03 | Worker 2 -> Worker 5 | `cairn-tools::invocation` now actively checks its durable record transitions against the shared domain validator. If you extend invocation outcomes or terminal states, update `cairn-domain::tool_invocation` first and let the tools tests tell you where the consumer path needs to follow.
- 2026-04-03 | Worker 2 -> Worker 4 | `cairn-runtime` run/task services now consume shared lifecycle transition helpers directly, and runtime integration tests expect the RFC 005 run path (`Pending -> Running -> Completed`) instead of the old looser behavior.
- 2026-04-03 | Worker 2 -> Worker 5 | `ToolInvocationRecord::{new_requested, mark_started, mark_finished}` are now the canonical durable mutation path in `cairn-domain`. Prefer those methods over cloning and mutating invocation records locally, and treat the store projection shape as the same shared domain record now.
- 2026-04-03 | Worker 2 -> Worker 3/8 | `cairn-store::InMemoryStore` now projects and reads tool invocations through the shared `ToolInvocationRecord` contract too. If you need test-store coverage for run timelines or API list endpoints, use `ToolInvocationReadModel` directly instead of rebuilding tool invocation state ad hoc.
- 2026-04-03 | Worker 2 -> Worker 3/8 | Postgres tool-invocation state now follows the same domain terminal-state mapping as in-memory and tools. If you surface canceled tool calls in API or graph/operator views, you can treat `canceled` as durable backend truth instead of a transient consumer-side inference.
- 2026-04-03 | Worker 2 -> Worker 3/8 | `SqliteAdapter` and `PgAdapter` now both implement `ToolInvocationReadModel`, so durable backend consumers can query tool invocation timelines through the same shared contract as the in-memory store. SQLite local-mode bootstrapping also now uses `sqlx::raw_sql`, so adapter-backed tests can rely on the full schema without statement-split drift.
- 2026-04-03 | Worker 2 -> Worker 3/4/8 | `SqliteAdapter` and `PgAdapter` now also implement `SessionReadModel` and `RunReadModel`, including `any_non_terminal` and `latest_root_run`. Durable backend callers can use the same current-state query contract across in-memory, SQLite, and Postgres instead of special-casing backend support.
- 2026-04-03 | Worker 2 -> Worker 3/4/8 | Durable backend parity now covers the rest of the runtime read models too: `TaskReadModel`, `ApprovalReadModel`, `CheckpointReadModel`, and `MailboxReadModel` now exist on both `SqliteAdapter` and `PgAdapter`. If API/runtime/SSE work needs durable reads instead of the in-memory store, treat those traits as backend-stable now.

## Ready For Review

- 2026-04-03 | Worker 2 | Review `crates/cairn-domain/*` for Week 1 shared-contract scaffold: IDs, tenancy keys, lifecycle helpers, policy types, runtime command/event envelopes including lease/subagent/recovery shapes, rollout selectors, prompt-release lifecycle, and provider routing records.
- 2026-04-03 | Worker 2 | Review Week 2 cut in `crates/cairn-domain/*`: `errors.rs`, `tool_invocation.rs`, `workers.rs`, provider route-decision invariant validation, and updated `lib.rs`/`ids.rs`. This is the narrow runtime-critical contract set intended to stay stable while Workers 3/4/5 integrate.
- 2026-04-03 | Worker 2 | Review follow-up invariant helpers in `crates/cairn-domain/src/tool_invocation.rs` and `crates/cairn-domain/src/workers.rs` before any runtime/store/tool worker duplicates that logic locally.
- 2026-04-03 | Worker 2 | Review explicit tool/external-worker command/event shells in `crates/cairn-domain/src/commands.rs` and `crates/cairn-domain/src/events.rs`; these are now part of the stable shared runtime cut.
- 2026-04-03 | Worker 2 | Review the new lifecycle transition helpers in `crates/cairn-domain/src/lifecycle.rs`; these are intended to be the canonical v1 run/task transition contract unless integration pain proves a gap.
- 2026-04-03 | Worker 2 | Review the new `project()` / `primary_entity_ref()` helpers in `crates/cairn-domain/src/commands.rs` and `crates/cairn-domain/src/events.rs`; they should replace crate-local enum introspection where possible.
- 2026-04-03 | Worker 2 | Review the new ownership conversions and envelope builders in `crates/cairn-domain/src/tenancy.rs`, `crates/cairn-domain/src/commands.rs`, and `crates/cairn-domain/src/events.rs`; these should replace ad hoc envelope assembly in downstream crates where possible.
- 2026-04-03 | Worker 2 | Review the payload-derived envelope helpers in `crates/cairn-domain/src/commands.rs` and `crates/cairn-domain/src/events.rs`, plus their first downstream uses in `crates/cairn-runtime/src/services/event_helpers.rs`, `crates/cairn-store/src/in_memory.rs`, and `crates/cairn-api/src/sse_publisher.rs`.
- 2026-04-03 | Worker 2 | Review the shared builder adoption in `crates/cairn-store/src/pg/event_log.rs` and `crates/cairn-store/src/sqlite/event_log.rs`; store backends should no longer hand-assemble `EventEnvelope` from row data when the canonical builder path is sufficient.
- 2026-04-03 | Worker 2 | Review the simplified runtime envelope path in `crates/cairn-runtime/src/services/event_helpers.rs` and the updated callsites in `run_impl.rs`, `session_impl.rs`, and `task_impl.rs`; Worker 2 now treats `RuntimeEvent` itself as the source of envelope ownership truth.
- 2026-04-03 | Worker 2 | Review the validator adoption in `crates/cairn-tools/src/invocation.rs`; the tools consumer path now depends on shared `ToolInvocation` transition and record-validation rules rather than only sharing the types.
- 2026-04-03 | Worker 2 | Review the lifecycle-helper adoption in `crates/cairn-runtime/src/services/run_impl.rs` and `crates/cairn-runtime/src/services/task_impl.rs`, plus the adjusted expectations in `crates/cairn-runtime/tests/lifecycle_integration.rs`; Worker 2 now expects runtime services to enforce the same shared transition rules published by `cairn-domain`.
- 2026-04-03 | Worker 2 | Review the domain-owned durable tool-invocation mutation helpers in `crates/cairn-domain/src/tool_invocation.rs`, their consumer adoption in `crates/cairn-tools/src/invocation.rs`, and the store projection cleanup in `crates/cairn-store/src/projections/tool_invocation.rs`; Worker 2 now expects tool invocation records to be mutated and exposed through one shared contract end-to-end.
- 2026-04-03 | Worker 2 | Review the new in-memory tool-invocation projection path in `crates/cairn-store/src/in_memory.rs` and the adjusted runtime integration expectation in `crates/cairn-runtime/tests/week3_integration.rs`; Worker 2 now expects the test store to expose the same shared `ToolInvocationReadModel` contract as the durable record layer.
- 2026-04-03 | Worker 2 | Review the terminal-state preservation fix in `crates/cairn-store/src/pg/projections.rs`; Postgres now stores `ToolInvocationOutcomeKind::Canceled` as `state = canceled` rather than flattening it into `failed`.
- 2026-04-03 | Worker 2 | Review the new durable backend read-model coverage in `crates/cairn-store/src/sqlite/adapter.rs` and `crates/cairn-store/src/pg/adapter.rs`, plus the SQLite schema-runner hardening in `crates/cairn-store/src/sqlite/adapter.rs`; Worker 2 now expects adapter-backed store paths to decode shared tool invocation records directly instead of leaving that contract in-memory-only.
- 2026-04-03 | Worker 2 | Review the new durable session/run read-model coverage in `crates/cairn-store/src/sqlite/adapter.rs` and `crates/cairn-store/src/pg/adapter.rs`; Worker 2 now expects current-state session/run queries to be backend-parity surfaces rather than in-memory- or SQLite-only conveniences.
- 2026-04-03 | Worker 2 | Review the new durable task/approval/checkpoint/mailbox read-model coverage in `crates/cairn-store/src/sqlite/adapter.rs` and `crates/cairn-store/src/pg/adapter.rs`, plus the SQLite adapter coverage in `sqlite_adapter_reads_task_approval_checkpoint_and_mailbox_models`; Worker 2 now expects the full runtime current-state query set to behave as a backend-parity contract.
