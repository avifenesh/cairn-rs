# Worker Core Mailbox

## Active Notes

- 2026-04-14 codex: Deeper-dive concerns to revisit later. (1) `TriggerService` is still app-local/in-memory in `crates/cairn-runtime/src/services/trigger_service.rs` + `crates/cairn-app/src/trigger_routes.rs`, while RFC 022 expects event-log durability. (2) `memory_proposal_hook` is constructed in `crates/cairn-app/src/lib.rs` and the builder exists in `crates/cairn-memory/src/api_impl.rs`, but the live memory/tool store paths still bypass that hook. (3) `working_dir_for_run()` in `crates/cairn-app/src/lib.rs` falls back to process cwd when no repo is allowlisted and chooses the first repo when many are allowlisted. (4) Product surface truth is still split across `crates/cairn-app/src/lib.rs` catalog routes and `crates/cairn-app/src/main.rs` binary-only routes.
- 2026-04-14 codex: Exploration findings only. `cargo test --workspace --quiet` passes. Doc drift spotted: `docs/design/rfcs/README.md` mislabels RFC 017 as Triggers and duplicates RFC 022, root `README.md` still says "13-crate workspace" while `Cargo.toml` has 19 members, and `ui/README.md` is still the default Vite template. No blocker.
- 2026-04-14 codex: Repo exploration/orientation pass only. Reading RFC index, manager replan, README, and representative crate entrypoints to map current Rust product composition and open seams. No blocker; will flag code/doc drift if found.
- 2026-04-11 14:55 worker-2: Prod-readiness hardening in `crates/cairn-app/src/main.rs`: adding SQLite-only `POST /v1/admin/backup`, threading the SQLite file path into `SqliteBackend`, and covering the non-SQLite path with a route-level test. Target gates: `cargo check --workspace` plus focused `cairn-app` test run before reporting.
- 2026-04-10 11:36 worker-2: HOLD per manager after Step 3 milestone 2 local verification. Do not commit until worker-3 clears the current `cairn-app/src/lib.rs` decision-route handler break and manager reconfirms `cargo check --workspace` is green. Step 3 service/state-machine code remains unchanged locally for immediate final gate rerun.
- 2026-04-10 11:34 worker-2: RFC 016 Step 3 milestone 2 is ready locally in `cairn-workspace`: `SandboxService` now owns provider resolution, in-memory lifecycle state, buffered `SandboxEvent` emission, policy hash capture, activation/heartbeat/checkpoint/preserve/destroy flows, and resource-exhaustion handling for `Destroy` / `PauseAwaitOperator` / `ReportOnly`. Local gates: `cargo test -p cairn-workspace --quiet` pass (14 tests), `cargo check -p cairn-workspace` pass, `cargo fmt --all` pass. Blocked from commit by cross-workstream `cargo check --workspace` failure in `crates/cairn-app/src/lib.rs` (missing decision route handlers: `list_decisions_handler`, `list_decision_cache_handler`, `get_decision_handler`, `invalidate_decision_handler`, `bulk_invalidate_decisions_handler`, `invalidate_by_rule_handler`). Manager notified with exact line refs.
- 2026-04-10 11:15 worker-2: RFC 016 Step 3 starting after manager verification of Step 2 commit `8f8280de`. Focus: `cairn-workspace` sandbox lifecycle core only — 9-state machine, `SandboxPolicy`/`ProvisionedSandbox` alignment, `SandboxProvider` trait surface, and RFC 016 `SandboxEvent` definitions/tests before provider-specific overlay/reflink work.
- 2026-04-10 11:06 worker-2: HOLD per manager after Step 2 local verification. Do not commit until worker-3 clears the current `cairn-runtime/src/decisions.rs` workspace break and manager reconfirms `cargo check --workspace` is green. Branch kept intentionally uncommitted for immediate final gate rerun once cleared.
- 2026-04-10 11:03 worker-2: RFC 016 Step 2 implementation is ready locally in `cairn-workspace`: `RepoCloneCache` now handles tenant-scoped path resolution, serialized ensure/refresh, readonly locking, and `cloned_set()` disk scans; `ProjectRepoAccessService` now uses the RFC-exact `HashMap<ProjectKey, Vec<RepoId>>` list API; `RepoStore::resolve()` enforces access-check-first before any clone work. Local gates: `cargo test -p cairn-workspace --quiet` pass (8 tests), `cargo check -p cairn-workspace` pass. Blocked from commit by cross-workstream `cargo check --workspace` failure in `cairn-runtime/src/decisions.rs` (`tokio`/`Notify`/`CacheEntry.notify` errors from worker-3 lane). Manager notified with exact error summary.
- 2026-04-10 10:44 worker-2: RFC 016 Step 1 complete and committed as `70faf291` (`workspace: scaffold cairn-workspace crate`). Gates: `cargo check -p cairn-workspace` pass, `cargo check --workspace` pass. Next: Step 2 implementation for `RepoCloneCache`, `ProjectRepoAccessService`, `RepoStore`, and `RepoStoreEvent` shape inside `cairn-workspace`.
- 2026-04-10 10:37 worker-2: RFC 016 Step 1 scaffold is built locally: new `crates/cairn-workspace` crate, workspace member wiring, module tree, stub public types, and placeholder `vendor/codex-linux-sandbox/VENDOR.md`. `cargo check -p cairn-workspace` passes. Step 1 is blocked from commit/finalization because `cargo check --workspace` currently fails outside my lane in shared `cairn-domain/src/decisions.rs` (`DecisionScopeRef` missing `Hash` derive for an existing downstream `Hash` derive).
- 2026-04-10 10:29 worker-2: RFC 016 Step 1 starting. Plan: checkpoint my verified Phase 0 files in a small commit, then scaffold `crates/cairn-workspace` only (module skeleton + Cargo wiring + stub public types), run `cargo check --workspace`, commit, and report.
- 2026-04-10 10:23 worker-2: Manager clarified RFC 016 prework boundary. When gated in, start with `cairn-workspace` crate skeleton + the two-service `RepoStore` split first. Keep `vendor/` empty for now except directory structure and a `VENDOR.md` placeholder; actual `codex-linux-sandbox` import is Phase 1 implementation work, not Phase 0.
- 2026-04-10 10:20 worker-2: HOLD per manager while worker-3 finishes Phase 0. Pre-read RFC 016 in detail and sketched planned `cairn-workspace` layout only, no code yet: `repo_store/` (`clone_cache`, `access_service`, `facade`, `sweep`), `sandbox/` (`types`, `policy`, `metadata`, `service`), `providers/` (`overlay`, `reflink`, shared trait), plus vendored `vendor/codex-linux-sandbox/`. Key boundary: `ActiveSandboxRepoSource` trait lives in workspace crate, impl later in `cairn-runtime`, startup wire-up later in `cairn-app`.
- 2026-04-10 10:12 worker-2: Phase 0 Task 0.1b + 0.3 landed. Added shared durability types in `crates/cairn-domain/src/recovery.rs`; added `ToolContext::buffer_event()` + `drain_buffered_events()` and buffered event storage in `crates/cairn-tools/src/builtins/mod.rs`. Verification: `cargo test -p cairn-domain recovery::tests --quiet`, `cargo test -p cairn-tools tool_context_buffers_and_drains_events --quiet`, and `cargo check --workspace` all passed.
- 2026-04-10 10:06 worker-2: Focused on Phase 0 shared foundation for RFC 016 + RFC 020. Implementing Task 0.1b in `crates/cairn-domain/src/recovery.rs` and then Task 0.3 in `crates/cairn-tools/src/builtins/mod.rs`. No blocker currently. Coordination note sent to worker-1 because their `contexts.rs` work may touch `crates/cairn-domain/src/lib.rs`, but `recovery.rs` already re-exports through the existing `pub use recovery::*`.
